# CI/CD simple project
## simple python web projecto with jenkins pipeline
In this project I make simple Jenkinsfile to automate the pipeline for clone this repositoriy then build am image from the Dockerfile then make the container of the image and run it at port 5000
